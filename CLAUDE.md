# Hypeline

의류 라이브 커머스 백엔드. 방송 중 한정 수량 드롭 판매를 다룬다.
컨셉과 규모 기준은 `docs/01-concept.md`, 레이어 규칙은 `docs/02-architecture.md`,
도메인 경계와 Redis 키·Kafka 토픽 규약은 `docs/03-domain.md` 를 먼저 읽는다.

---

## 커밋 전 금지 사항 (최우선)

이 레포는 **Public** 이다. 아래에 해당하는 내용은 어떤 이유로도 커밋하지 않는다.

### 절대 커밋 금지

- 실제 인증 정보 — API 키, 액세스 토큰, 시크릿, 비밀번호, 개인키(`*.pem`, `*.key`, `id_rsa*`), 키스토어(`*.jks`, `*.p12`)
- 외부 서비스 엔드포인트에 붙는 자격증명 — Slack webhook URL, PG 상점키, AWS 자격증명, DB 접속 계정
- 개인정보 — 실명, 주민등록번호, 전화번호, 주소, 카드번호, 회사 내부 이메일
- 회사 자산 — 사내 도메인·호스트명·IP, 내부 시스템 이름, 사내 문서 인용
- 로컬 절대경로 (`/Users/<이름>/...`) 와 그로부터 드러나는 계정명
- 실제 사용자 데이터가 담긴 덤프·로그·스크린샷

### 대신 이렇게 한다

- 운영 값은 전부 환경변수로 받는다 — `${MYSQL_PWD}` 처럼 두고 값은 커밋하지 않는다
- `local`, `test` 프로파일에 한해 더미 값(`application/application`)을 둘 수 있다. `dev`, `qa`, `prd` 프로파일에는 어떤 실제 값도 넣지 않는다
- 예시가 필요하면 `.env.example` 에 **키 이름만** 적는다. `.env` 자체는 `.gitignore` 대상이다
- 테스트 픽스처의 인적 정보는 명백한 가짜를 쓴다 (`홍길동`, `test@example.com`, `010-0000-0000`)

### 커밋 전 확인

새 파일을 커밋에 포함하기 전에 `git diff --staged` 로 실제 내용을 확인한다.
설정 파일(`*.yml`, `*.xml`, `*.properties`)이나 스크립트를 추가·수정했다면 특히 값을 눈으로 본다.

이미 올라간 뒤에는 커밋을 되돌려도 GitHub 에 남는다. 노출됐다면 되돌리기 전에 **해당 자격증명부터 폐기·재발급**한다.

---

## 작업 규칙

### 커밋

- 커밋 메시지는 한국어로 쓴다. 형식은 `type: 요약` (`feat`, `fix`, `refactor`, `test`, `docs`, `chore`)
- 커밋 신원은 이 레포에 로컬로 고정돼 있다 (`jsj1215 <57480220+jsj1215@users.noreply.github.com>`). 바꾸지 않는다
- 사용자가 요청하지 않으면 커밋·푸시하지 않는다

### 코드

- Java 21, Spring Boot 3.4. 모든 서브모듈은 toolchain 으로 JDK 21 에 고정돼 있다
- 의존 방향은 `domain` 을 향한다. `domain` 이 저장소 인터페이스를 선언하고 `infrastructure` 가 구현한다
- `domain` 에 스프링·JPA 외 프레임워크를 끌어들이지 않는다
- 판매 단위는 상품이 아니라 **SKU(상품 × 사이즈 × 컬러)** 다. 재고는 SKU 단위로 잡는다
- 재고·대기열 Redis 키는 evict 되면 안 된다. `maxmemory-policy` 는 `noeviction` 을 유지한다

### 문서

- 성능 개선을 다룰 때는 개선 전후 수치를 함께 남긴다. "빨라졌다" 같은 서술은 쓰지 않는다
- 부하 테스트 결과는 어떤 프로파일·인스턴스 수로 측정했는지 함께 적는다. 조건 없는 수치는 근거가 되지 않는다
